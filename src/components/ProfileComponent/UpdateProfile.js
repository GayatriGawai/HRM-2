import React, { Fragment } from 'react';

const UpdateProfile = () => {
    return (
        <Fragment>
            <div>UpdateProfile</div>
        </Fragment>
    );
};

export default UpdateProfile;
