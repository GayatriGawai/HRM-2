import React, { Fragment } from 'react';
import Sidebar from '../Navigation/sidebar';

const CreateNews = () => {
    return (
        <Fragment>
            <Sidebar />
            <div>CreateNews</div>
        </Fragment>
    );
};

export default CreateNews;
