import React, { Fragment } from 'react';
import Sidebar from '../Navigation/sidebar';

const HomePage = () => {
    return (
        <Fragment>
            <Sidebar />
        </Fragment>
    );
};

export default HomePage;
